[project]
name = "globalflow-worker"
version = "0.1.0"
description = "Cloudflare Workers deployment for GlobalFlow"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.110",
  "jinja2>=3.1",
  "httpx>=0.25",
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
